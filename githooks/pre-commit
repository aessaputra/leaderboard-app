#!/usr/bin/env sh
# Simple pre-commit hook without extra deps. Enable with:
#   git config core.hooksPath githooks

set -e

echo "✓ Running lint..."
pnpm lint

echo "✓ Running typecheck..."
pnpm typecheck

echo "✓ OK"

